import { StyleSheet } from 'react-native';

export const formStyles = StyleSheet.create({
  inputBase: {
    borderWidth: 1,
    padding: 16,
    marginBottom: 16,
    fontSize: 16,
  },
  buttonRow: {
    flexDirection: 'row',
    gap: 10,
    marginTop: 20,
  },
  buttonSlot: {
    flex: 1,
  },
});
