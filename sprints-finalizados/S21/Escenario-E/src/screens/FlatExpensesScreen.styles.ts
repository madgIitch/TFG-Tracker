// src/screens/FlatExpensesScreen.styles.ts
import { StyleSheet } from 'react-native';
import { Theme } from '../theme';

export const makeStyles = (theme: Theme) => StyleSheet.create({
  container: { flex: 1, backgroundColor: theme.colors.systemBackground },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingHorizontal: 24,
    paddingVertical: 16,
    backgroundColor: theme.colors.glassPanel,
  },
  headerCenter: { flex: 1, marginHorizontal: 12 },
  headerTitle: { fontSize: 18, fontWeight: '600' },
  headerSubtitle: { fontSize: 12, color: theme.colors.textSecondary, marginTop: 2 },
  headerAction: { flexDirection: 'row', alignItems: 'center', gap: 4 },
  headerActionText: { fontSize: 13, fontWeight: '600', color: theme.colors.primary },
  loadingContainer: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  loadingText: { marginTop: 10, fontSize: 14, color: theme.colors.textSecondary },
  content: { padding: 20, gap: 12 },

  // Summary card
  summaryCard: {
    backgroundColor: theme.colors.glassBackground,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: theme.colors.glassBorder,
    padding: 20,
    shadowColor: '#7C3AED',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.08,
    shadowRadius: 12,
    elevation: 3,
  },
  summaryRow: { flexDirection: 'row', alignItems: 'center', marginBottom: 16 },
  summaryItem: { flex: 1, alignItems: 'center' },
  summaryValue: { fontSize: 22, fontWeight: '700', color: theme.colors.text },
  summaryLabel: { fontSize: 12, color: theme.colors.textSecondary, marginTop: 2 },
  summaryDivider: { width: 1, height: 40, backgroundColor: theme.colors.separator },
  settlementButton: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 6,
    paddingVertical: 10,
    borderRadius: 12,
    backgroundColor: 'rgba(124, 58, 237, 0.10)',
  },
  settlementButtonText: { fontSize: 14, fontWeight: '600', color: theme.colors.primary },

  // Empty state
  emptyState: { alignItems: 'center', paddingVertical: 48 },
  emptyTitle: { fontSize: 16, fontWeight: '600', color: theme.colors.text, marginTop: 16 },
  emptySubtitle: { fontSize: 13, color: theme.colors.textSecondary, marginTop: 6, textAlign: 'center' },

  // Expense cards
  expenseCard: {
    backgroundColor: theme.colors.glassBackground,
    borderRadius: 16,
    borderWidth: 1,
    borderColor: theme.colors.glassBorder,
    padding: 16,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.04,
    shadowRadius: 8,
    elevation: 1,
  },
  expenseHeader: { flexDirection: 'row', alignItems: 'flex-start', gap: 10 },
  expenseIconWrap: {
    width: 36,
    height: 36,
    borderRadius: 10,
    backgroundColor: 'rgba(124, 58, 237, 0.10)',
    alignItems: 'center',
    justifyContent: 'center',
  },
  expenseInfo: { flex: 1 },
  expenseDescription: { fontSize: 15, fontWeight: '600', color: theme.colors.text },
  expenseMeta: { fontSize: 12, color: theme.colors.textSecondary, marginTop: 2 },
  expenseAmount: { fontSize: 16, fontWeight: '700', color: theme.colors.text },
  splitsList: { marginTop: 12, gap: 4 },
  splitRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingVertical: 4,
    borderTopWidth: 1,
    borderTopColor: theme.colors.separator,
  },
  splitName: { fontSize: 13, color: theme.colors.textSecondary },
  splitAmount: { fontSize: 13, fontWeight: '600', color: theme.colors.textSecondary },
  splitEqual: { marginTop: 10, fontSize: 12, color: theme.colors.textSecondary },

  // Modal
  modalOverlay: {
    flex: 1,
    justifyContent: 'flex-end',
    backgroundColor: 'rgba(0,0,0,0.4)',
  },
  modalSheet: {
    backgroundColor: theme.colors.surface,
    borderTopLeftRadius: 28,
    borderTopRightRadius: 28,
    padding: 24,
    paddingBottom: 36,
    gap: 12,
  },
  modalHandle: {
    width: 40,
    height: 4,
    borderRadius: 2,
    backgroundColor: theme.colors.border,
    alignSelf: 'center',
    marginBottom: 8,
  },
  modalTitle: { fontSize: 20, fontWeight: '700', color: theme.colors.text, marginBottom: 4 },
  fieldLabel: { fontSize: 13, fontWeight: '600', color: theme.colors.textSecondary, marginBottom: -4 },
  input: {
    borderWidth: 1,
    borderColor: theme.colors.glassBorder,
    borderRadius: 12,
    paddingHorizontal: 14,
    paddingVertical: 12,
    fontSize: 15,
    color: theme.colors.text,
    backgroundColor: theme.colors.surfaceLight,
  },
  memberChips: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  memberChip: {
    paddingHorizontal: 14,
    paddingVertical: 8,
    borderRadius: 999,
    borderWidth: 1,
    borderColor: theme.colors.glassBorder,
    backgroundColor: theme.colors.glassBackground,
  },
  memberChipSelected: { borderColor: theme.colors.primary, backgroundColor: 'rgba(124, 58, 237, 0.10)' },
  memberChipText: { fontSize: 13, fontWeight: '500', color: theme.colors.textSecondary },
  memberChipTextSelected: { color: theme.colors.primary },
  splitPreview: {
    fontSize: 13,
    color: theme.colors.primary,
    fontWeight: '500',
    textAlign: 'center',
  },
  modalActions: { flexDirection: 'row', gap: 12, marginTop: 8 },
  cancelButton: {
    flex: 1,
    paddingVertical: 14,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: theme.colors.glassBorder,
    alignItems: 'center',
    backgroundColor: theme.colors.surfaceLight,
  },
  cancelButtonText: { fontSize: 15, fontWeight: '600', color: theme.colors.textSecondary },
  confirmButton: {
    flex: 2,
    paddingVertical: 14,
    borderRadius: 14,
    backgroundColor: theme.colors.primary,
    alignItems: 'center',
    justifyContent: 'center',
  },
  confirmButtonDisabled: { opacity: 0.6 },
  confirmButtonText: { fontSize: 15, fontWeight: '700', color: '#FFFFFF' },
});
